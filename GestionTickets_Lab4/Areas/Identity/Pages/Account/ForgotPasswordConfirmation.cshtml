@page
@model GestionTickets_Lab4.Areas.Identity.Pages.Account.ForgotPasswordConfirmationModel
@{
    ViewData["Title"] = "Correo Enviado";
}

<style>
    .confirm-bg {
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        min-height: calc(100vh - 140px);
        display: flex;
        align-items: center;
        justify-content: center;
        padding-top: 20px;
        padding-bottom: 20px;
    }

    .confirm-card {
        border: none;
        border-radius: 15px;
        box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        background-color: rgba(255, 255, 255, 0.95);
        overflow: hidden;
        max-width: 500px; /* Un poco más ancha para que el texto se lea bien */
    }

    .confirm-header {
        background-color: #ffc107; /* Mismo Naranja que la pantalla anterior */
        color: #212529;
        padding: 20px;
        text-align: center;
    }
</style>

<div class="confirm-bg">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">

                <div class="card confirm-card animate__animated animate__fadeInDown">
                    <div class="confirm-header">
                        <h3 class="mb-0 fw-bold"><i class="bi bi-envelope-check-fill"></i> ¡Revise su correo!</h3>
                    </div>

                    <div class="card-body p-5 text-center">

                        <div class="mb-4">
                            <i class="bi bi-check-circle text-success" style="font-size: 3rem;"></i>
                        </div>

                        <h5 class="card-title fw-bold mb-3">Solicitud Procesada</h5>

                        <p class="text-muted mb-4">
                            Por favor, revise su bandeja de entrada. Si tiene una cuenta con nosotros, le hemos enviado un correo con las instrucciones para restablecer su contraseña.
                        </p>

                        <div class="d-grid gap-2">
                            <a asp-page="./Login" class="btn btn-outline-dark btn-lg">
                                <i class="bi bi-box-arrow-in-right"></i> Volver al Inicio de Sesión
                            </a>
                        </div>

                    </div>
                </div>

                <div class="text-center mt-3 text-muted small">
                    &copy; 2026 - Clínica UTN Salud
                </div>

            </div>
        </div>
    </div>
</div>